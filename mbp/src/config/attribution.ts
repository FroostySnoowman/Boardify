/**
 * Toggle for third-party brand attribution in the app (e.g. GIF provider).
 * 1 = show attribution (required for production with KLIPY API)
 * 0 = hide attribution
 */
export const KLIPY_ATTRIBUTION_ENABLED = 1;
